import React, { Component } from 'react';
import Base from 'terra-base';

class App extends Component {
  render() {
    return (
      <Base className="custom-App" locale="en">
        Edit code in <code>src/App.js</code> to get started.
      </Base>
    );
  }
}

export default App;